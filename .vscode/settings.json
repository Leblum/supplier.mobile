// Place your settings in this file to overwrite default and user settings.
//"--require ts-node/register --reporter spec --watch-extensions ts"
{
    "workbench.editor.enablePreview": false,
    "files.exclude": {
        "**/*.js": { "when": "$(basename).ts" },
        "**/*.css": { "when": "$(basename).scss" }
    }
}